# Presets Directory

This directory contains predefined configuration data that can be used throughout the application.

## Usage Guidelines

Preset files in this directory supply default configuration. User overrides belong in Preference, not here.
